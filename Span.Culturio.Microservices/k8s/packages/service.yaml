apiVersion: v1
kind: Service
metadata:
  name: packages-service
  namespace: culturio
spec:
  selector:
    app: packages-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP