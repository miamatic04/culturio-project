apiVersion: v1
kind: Service
metadata:
  name: cultureobjects-service
  namespace: culturio
spec:
  selector:
    app: cultureobjects-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP