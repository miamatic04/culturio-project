apiVersion: v1
kind: Service
metadata:
  name: users-service
  namespace: culturio
spec:
  selector:
    app: users-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP