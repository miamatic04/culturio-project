apiVersion: v1
kind: Service
metadata:
  name: subscriptions-service
  namespace: culturio
spec:
  selector:
    app: subscriptions-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP