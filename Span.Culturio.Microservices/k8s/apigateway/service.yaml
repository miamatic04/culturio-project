apiVersion: v1
kind: Service
metadata:
  name: apigateway
  namespace: culturio
spec:
  selector:
    app: apigateway
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP